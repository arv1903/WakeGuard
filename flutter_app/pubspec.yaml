name: driver_monitor

description: Flutter dashboard for the Driver Drowsiness & Distraction Detection service.
publish_to: "none"
version: 0.2.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^0.69.2
  intl: ^0.19.0
  shared_preferences: ^2.2.2
  qr_flutter: ^4.1.0
  mobile_scanner: ^7.4.1

dev_dependencies:
  fake_async: ^1.3.3
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
